# `.claude/` — Claude Code project config

**Human-readable map:** [docs/phase-00/STARTER-KIT.md](../docs/phase-00/STARTER-KIT.md) — what each **command**, **skill**, and **agent** is for.

- **`commands/`** — `/intention`, `/prd`, `/plan`, `/run-plan`
- **`skills/`** — Turbo monorepo discipline, PRD alignment
- **`agents/`** — monorepo builder + four track app builders

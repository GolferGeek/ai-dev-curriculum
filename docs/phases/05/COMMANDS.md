# Commands — Phase 05

```text
/intention → /prd → /plan → /run-plan
/skill-scout
/skill-evaluate <skill revision>
/scan-errors skills-browser
/commit pr
```

`/skill-scout` discovers but never installs. `/skill-evaluate` recommends but never approves or publishes. Phase 05.5 introduces `/skill-publish` and `/skill-maintain`.

{
  "name": "ai-dev-curriculum",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "postinstall": "PLAYWRIGHT_BROWSERS_PATH=0 playwright install chromium"
  },
  "devDependencies": {
    "playwright": "^1.49.1",
    "turbo": "^2.4.4"
  },
  "packageManager": "npm@10.9.0",
  "workspaces": ["apps/*", "packages/*"]
}

# SurrealDB connection
SURREAL_URL=http://127.0.0.1:8000
SURREAL_NS=quickbooks
SURREAL_DB=main

# Root credentials (used by schema scripts, seed scripts, and getRootConnection)
SURREAL_ROOT_USER=root
SURREAL_ROOT_PASS=root
